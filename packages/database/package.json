{
  "name": "@brainstack/database",
  "version": "0.1.0",
  "private": true,
  "description": "PostgreSQL schema, SQL migrations, and the migration runner for BrainStack AI Receptionist",
  "scripts": {
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
