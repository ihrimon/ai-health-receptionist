{
  "name": "brainstack-booking-agent",
  "version": "0.1.0",
  "private": true,
  "description": "BrainStack AI Receptionist - AI Voice & Chat Booking Agent (Monorepo)",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=20"
  }
}
