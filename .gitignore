# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/
.turbo/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Testing
coverage/

# Database
*.sqlite

# Misc
.cache/

# Markdown docs (not published to GitHub, except the root README)
*.md
!README.md
# ...except these two — read from disk at runtime (LlmChatClient's system
# prompt, and the RAG knowledge base source), not just documentation. A
# deploy built from a git checkout (e.g. Render) is missing them entirely
# without this, so every chat message 500s on fs.readFileSync ENOENT.
!packages/ai/prompts/booking-conversation.md
!packages/ai/knowledge/faq.md
