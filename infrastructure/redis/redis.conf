# Minimal Redis config for local/dev use with BullMQ.
# Mounted only if you switch the compose service from `command:` to
# `--include infrastructure/redis/redis.conf`; the default compose service
# passes `--appendonly yes` directly instead.

appendonly yes
appendfsync everysec
